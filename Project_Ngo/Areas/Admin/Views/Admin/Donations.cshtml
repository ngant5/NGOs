@{
    ViewBag.Title = "Donations";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
    List<Project_Ngo.Models.Entities.Donations> don = new List<Project_Ngo.Models.Entities.Donations>();
    if (ViewBag.donations != null) { don = ViewBag.donations; }
}

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
    <style type='text/css'>
        #numbered-table {
            counter-reset: rowNumber; /* Reset counter for each table */
        }

            #numbered-table tbody tr {
                counter-increment: rowNumber; /* Increment counter for each row */
            }

                #numbered-table tbody tr td:first-child::before {
                    content: counter(rowNumber); /* Display row number */
                    min-width: 1em; /* Ensure space for at least one digit */
                    margin-right: 0.5em; /* Add some spacing between number and content */
                }
    </style>
</head>
<body>

    <div>
        <table class="table table-striped" width="700" border="1" id="numbered-table">
            <thead>
                <tr>
                    <th>stt</th>
                    <th>Campaign ID</th>
                    <th>Total Amount</th>
                    <th>Donation Date</th>
                    <th>Payment method</th>
                    <th>Status</th>
                    <th>Notes</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var group in don.GroupBy(d => d.CampaignsID))
                {
                    <tr>
                        <td></td>
                        <td><a href="@Url.Action("CampaignDetails", new { id = group.Key })">@group.Key</a></td>
                        <td>@group.Sum(d => d.Amount)</td>
                        <td>@group.First().DonationDate</td>
                        <td>@group.First().PaymentMethod</td>
                        <td>@group.First().Status</td>
                        <td>@group.First().Note</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

</body>
</html>
